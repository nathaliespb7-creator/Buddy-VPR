# ✅ Финальный чек-лист перед релизом

**Дата:** 27.02.2026  
**Статус по кодовой базе и деплою.**

---

## Код и интеграция

| Пункт | Статус | Комментарий |
|-------|--------|-------------|
| Все 11 островов загружены в `/data/` | ✅ | **11 вариантов** на карте: кнопка «Все острова» (смешанный режим) + **10 островов** по категориям (accent, phonetics, morphemics, morphology, syntax, vocabulary, context, plan, reading, meaning). Данные: `client/src/data/` — `sentences.ts`, `contextIsland.ts`, `phraseologyWisdom.ts`; основной массив в `lib/taskData.ts`. |
| Все валидаторы работают корректно | ✅ | `vprValidator.ts` (основная мысль, контекст, фразеологизмы, синонимы), `syntaxValidator.ts` (остров предложений). TaskCard вызывает валидацию по `task.category` и `variant: "context"`. |
| Аналитика отображает прогресс по каждому острову | ✅ | `taskCounts` по категориям из `allTasks`; прогресс по островам — API `/api/categories/progress` + ProgressIndicator в IslandMap. Офлайн — только звёзды в профиле. |
| Прогноз оценки ВПР рассчитывается верно | ✅ | На экране завершения раунда: процент → оценка 2–5 (0–44%→2, 45–59%→3, 60–74%→4, 75%+→5). Итоговый балл 0–24 по ВПР-спецификации в коде не выводится отдельно. |

---

## Тестирование

| Пункт | Статус | Комментарий |
|-------|--------|-------------|
| Пройдено тестовое задание на каждом острове | 🔲 Ручная проверка | Запустить приложение, открыть карту, зайти на каждый остров и решить 1 задание. |
| Проверена штрафная система подсказок | ✅ | Золотая звезда — без подсказки, серебряная — с подсказкой (TaskCard, starCalculation). |
| Проверено сохранение прогресса (localStorage) | ✅ | `buddy_profile`, `buddy_session_id`; профиль сохраняется после звёзд, миграция в profileMigration. |
| Проверена адаптация под мобильные | ✅ | Mobile-first, кнопки ≥44px, safe-area, Header с вариантом `task` на малых экранах. Проверить на устройстве. |

---

## Деплой

| Пункт | Статус | Комментарий |
|-------|--------|-------------|
| `npm run build` без ошибок | ⚠️ | **`npm run build:client`** (Vite) — **успешен**. Полный `npm run build` может падать в среде из-за tsx/pipe; на Vercel используется сборка клиента. |
| Деплой на Vercel успешен | 🔲 Ручная проверка | В панели Vercel запустить деплой, убедиться в успешном билде. |
| SSL-сертификат активен | ✅ | Vercel выдаёт HTTPS по умолчанию. Проверить https://www.buddyvpr.ru вручную. |
| Все 752 задания доступны | ✅ | **752 задания**: inline в taskData (~610) + syntax (55) + phraseologyWisdom (27) + contextIsland (**60**). В Остров контекста добавлено 10 заданий (итого 60). |

---

## Аналитика

| Пункт | Статус | Комментарий |
|-------|--------|-------------|
| Dashboard отображает все 11 островов | ✅ | **Карта знаний (IslandMap)** — дашборд: кнопка «Все острова» + 10 островов с подписями и счётчиками заданий. Прогресс по островам — в карточках (ProgressIndicator). |
| Прогресс-бары обновляются в реальном времени | ✅ | В раунде — TaskProgressBar в Header (текущее/всего). По островам — после ответа API `/api/categories/progress` и при возврате на карту. |
| Прогноз оценки ВПР рассчитывается корректно | ✅ | На экране завершения раунда: процент правильных → оценка 2–5 и строка «Баллов: N из M». |
| Рекомендации отображаются динамически | ⚠️ | Рекомендации в виде «слова для повторения» на экране завершения (wrongWords). Отдельного блока «рекомендации по островам» в UI нет — только прогресс по категориям на карте. |

---

## Итог

- **Готово по коду:** валидаторы, прогресс по островам, оценка 2–5 на экране завершения, экспорт JSON, таймер 45 мин, цвет сложности в TaskCard, сборка клиента.
- **Сделано дополнительно:** 10 заданий в Остров контекста (60 вместо 50) — всего **752 задания**. 11 пунктов на карте: «Все острова» + 10 островов.
- **Проверить вручную:** по одному заданию на каждом острове, деплой на Vercel, SSL и загрузка на реальном устройстве.
